const {config} = require("../../config/config");

const SEND_EMAIL_QUEUE_ID = "sendEmail";
const SEND_EMAIL_CREATOR_QUEUE_ID = "sendEmailCreator";

module.exports = {
    SEND_EMAIL_QUEUE_ID,
    SEND_EMAIL_CREATOR_QUEUE_ID,
}